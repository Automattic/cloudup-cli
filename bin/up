#!/usr/bin/env node

/**
 * Module dependencies.
 */

var program = require('commander')
  , up = require('..');

program
  .usage('[options] [file ...]')
  .parse(process.argv);

var view = up.view();

view.add({ name: 'foo' })
view.add({ name: 'bar' })
view.add({ name: 'baz' })
view.draw();
